package com.manishm.mobilespec

import com.manishm.mobilespec.engine.BackendPolicy

/** Generated only from a qualification report whose every required gate passed. */
internal object BundledBackendPolicy {
    const val status = "disabled: no qualified GPU or hybrid policy"
    val value: BackendPolicy? = null
}
