# Android / Gradle
.gradle/
.kotlin/
build/
local.properties
*.apk
*.aab
.cxx/
captures/

# IDE
.idea/
*.iml
.vscode/

# Python tooling
__pycache__/
*.py[cod]

# Native build output
out/
build-android-*/
build-host/
dist/

# Model weights — never commit (see models/manifest.json)
*.gguf
*.bin
*.safetensors

# Large raw benchmark bundles (keep small summaries only)
benchmarks/results/raw/*
# Final promoted phase-aware sweep. This single report contains every raw
# sample, command, stderr/exit classification, gate, and provenance field.
!benchmarks/results/raw/20260807-232905-autotune/
!benchmarks/results/raw/20260807-232905-autotune/autotune.json

# Autotuner profile cache — machine-local state keyed by (device, model).
# The curated, shareable table is benchmarks/profiles/known-profiles.json.
benchmarks/results/autotune-cache.json

# OS junk
Thumbs.db
.DS_Store
