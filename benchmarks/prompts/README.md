# Fixed benchmark prompts

Deterministic, versioned prompts. Do not edit after first published result — add new files instead.

- `prompt_short.txt` — ~128-token prompt (trim to exact count with the model tokenizer in Phase 1)
- `prompt_long.txt` — ~512-token prompt

llama-bench synthetic pp/tg workloads don't use these; they are for llama-cli end-to-end and in-app runs.
